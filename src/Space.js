import react from 'react';

export default function Line({key, margin}) {
    return (
        <div 
            className = 'space'
            style={{
                margin: margin
            }}
        >
        </div>
    )
}